import 'package:http/http.dart' as http;

import 'package:Snacked/data/models/watch_data.dart';


// fetch data from server via HTTP 

// Future<WatchData> fetchData() async {
//   final response = await http.get(Uri.parse('Power Automate url here'));
      // might need to use POST instead?
      // if Power Automate flow requires request body to start
      // request to perform action instead of just data lookup
  

//   return WatchData thisss; 

  // if can't fetch or can't populate WatchData: noData = true;

// }



